# Database Configuration for Production
# You'll need to set this in Replit's Environment Variables
DATABASE_URL=postgresql://username:password@hostname:port/database

# Optional: Set environment
NODE_ENV=production
